include ../button

mixin contact-form
  section(class="col-span-full mt-[10rem] grid grid-cols-subgrid")
    div(
      class="col-[main] flex flex-wrap items-end justify-between gap-x-6 gap-y-2 ~mb-8/12"
    )
      h2(class="text-h2 font-medium text-brand") Возникли вопросы или&nbsp;предложения?
    form(
      action="/",
      method="POST",
      novalidate="",
      class="col-[main] grid grid-cols-subgrid"
    )
      div(class="col-span-full flex justify-between gap-12")
        div(class="flex-1", data-form-control="")
          input(
            type="text",
            placeholder="Ваше имя",
            required="",
            pattern="[А-ЯЁа-яё ]{2,}",
            data-pattern-text="Введите имя минимум из 2 букв кириллицей",
            class="peer h-16 w-full border border-muted-background px-6 placeholder:text-muted-background focus:outline-none"
          )
          p(
            class="text-red-500 peer-aria-[invalid=true]:mt-2",
            data-form-control-message=""
          ) 
        div(class="flex-1", data-form-control="")
          input(
            type="tel",
            placeholder="Номер телефона",
            required="",
            pattern="\\+7\\s\\([0-9]{3}\\)\\s[0-9]{3}-[0-9]{2}-[0-9]{2}",
            data-pattern-text="Перепроверьте правильность номера телефона, мы позвоним на него",
            class="peer h-16 w-full border border-muted-background px-6 placeholder:text-muted-background focus:outline-none"
          )
          p(
            class="text-red-500 peer-aria-[invalid=true]:mt-2",
            data-form-control-message=""
          ) 
        +button("default", "default")(type="submit") Оставить заявку
      p(class="col-span-12 mt-4 text-muted-background") При нажатии на&nbsp;кнопку &laquo;Оставить заявку&raquo; вы&nbsp;соглашаетесь с&nbsp;нашей политикой обработки персональных данных
